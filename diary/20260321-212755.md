# 2026-03-21 21:27:55

## 実施内容

- GitHub Issue / PR を後続処理で共通利用できる `ResolvedTarget` を追加した。
- `target_url` または `repository_full_name` と `target_type` / `target_number` から GitHub target を解決する処理を追加した。
- CLI の preflight 後に target 解決を配線し、確認用出力に `target=org/repo#number` を含めるようにした。

## レビュー対応

- 自動レビューで指摘された、非 GitHub の `target_url` を成功扱いしてしまう経路を修正した。
- 今回未対応の URL は日本語の入力エラーとして終了するようにした。

## 手動確認

- `./.venv/bin/python -m compileall src`
- `VV_OPENAI_API_KEY=dummy /home/hihok/Github/vv-ai/.venv/bin/python -m vv_ai --command plan --instruction test --target-url https://github.com/octo/test/issues/12`
- `VV_OPENAI_API_KEY=dummy /home/hihok/Github/vv-ai/.venv/bin/python -m vv_ai --command review --instruction test --target-url 'https://github.com/octo/test/pull/34/?x=1#discussion_r1'`
- `VV_OPENAI_API_KEY=dummy /home/hihok/Github/vv-ai/.venv/bin/python -m vv_ai --event issue_comment --event-file /tmp/vv-ai-target-D4Qa1H/issue_comment_pr.json`
- `VV_OPENAI_API_KEY=dummy /home/hihok/Github/vv-ai/.venv/bin/python -m vv_ai --event workflow_dispatch --event-file /tmp/vv-ai-target-D4Qa1H/workflow_dispatch_issue.json`
- `VV_OPENAI_API_KEY=dummy /home/hihok/Github/vv-ai/.venv/bin/python -m vv_ai --command plan --instruction test --target-url https://github.com/octo/test/pull/34/files`
- `VV_OPENAI_API_KEY=dummy /home/hihok/Github/vv-ai/.venv/bin/python -m vv_ai --command plan --instruction test --target-url https://example.com/foo`

## 次の作業

- `plan.md` の次の未完了タスクである「ローカルパスを target として解決できるようにする」に進む。
