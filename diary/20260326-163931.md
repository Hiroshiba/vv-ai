# 2026-03-26 review コマンド実装

## タスク

plan.md セクション9「review コマンドを実装する」。

## 実装内容

`prompt.py` に `"review"` のタスク説明を追加、`command_handler.py` に PR 限定バリデーション（`CommandError`）と後処理への `"review"` 追加。変更は2ファイル・約15行。

## レビューと反映

`_post_response_comment` の docstring が `"reply / plan"` のままで `"review"` が反映されていないとの指摘。修正した。

見逃した理由: `_handle_post_execution` に `"review"` を追加した際、呼び出し先の `_post_response_comment` の docstring まで更新する意識が抜けた。変更した側と呼ばれた側を別々に意識していたため。

## 手こずったこと

テスト環境に `VV_AI_AGE_PUBLIC_KEY` と API key がないため、`CommandError` が Issue target で正しく送出されることを CLI 出力から直接確認できなかった。コードの論理的な正しさで判断した。
