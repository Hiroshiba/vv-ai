# 2026-03-24 13:02:51

## 実施内容

- `Claude Code CLI 実行ラッパーを実装する` 周辺として、Claude 実行、session restore、artifact cleanup、resume 継続性の修正を進めた。
- 途中で複数のレビュー指摘を受けて、security、session restore、cleanup、provider 選択の修正を重ねた。
- その後、レビュー反映が plan と目的からずれていないかを見直した。
- 最終的に今回の差分は方針がぶれていたため、ユーザー判断で全変更を revert した。
- その状態を前提に、今回は日誌だけを追加して終了する。

## 受けたレビュー

- Claude 起動時に repo 側 settings を読めてしまう security 問題。
- local manifest と remote artifact fallback の不整合。
- interrupt 時に最新 provider session を保存できない問題。
- local restore 後に平文 session が残る cleanup 問題。
- cleanup 対象が restore root 全体ではなく `session` 配下だけだった問題。
- remote fallback 後 manifest の resume 前提を再検証していなかった問題。
- `codex` を未実装なのに自動選択へ戻してしまう provider 選択の退行。
- artifact 不在時にも local manifest だけで継続できる経路を remote 依存にしてしまった退行。

## レビュー対応

- Claude security、session restore 整合、interrupt 時の session 保存、cleanup まわりは個別には妥当な修正を入れた。
- 一方で `codex` の扱いは、review ごとに `supports_execution` や自動選択条件を出し入れしてしまい、公開 surface と実装状態の契約を一貫して保てなかった。
- さらに session restore でも、artifact が無くても継続できる経路まで remote fallback 必須に寄せてしまう場面があった。
- 結果として、レビューコメント単位では辻褄が合っても、plan 全体では不安定な差分になった。
- 最後は全変更を revert したため、コードには何も残していない。

## 見逃しの考察

- 一番の失敗は、`.codex/skills/review-triage/SKILL.md` の「契約面単位で扱う」を徹底できなかったことだった。
- `provider selection / execution contract` や `resume without artifact` のような契約面で固定せず、レビュー comment 単位で局所反応した。
- そのため、各コメントの局所的な正しさに引っ張られ、`requirements.md`、`plan.md`、現在の実装状態の三者整合を途中で崩した。
- 特に `codex` は、要件では公開された provider なのに実行本体は未実装という既存の不整合があり、本来はここを明示的に整理するか保留にすべきだった。
- にもかかわらず、レビューを跳ね返さずその場しのぎで整合を取り続けたため、方針が往復した。

## 手こずったこと

- Claude と session 周辺のレビューは個別にはもっともで、どこまで受けると plan 全体を壊すかの境界判断が難しかった。
- `codex` は requirements と現在実装のズレが根本にあり、レビューを直すだけでは収束しなかった。
- 途中からはコードを直すことより、どの契約を優先するかの整理不足が一番の詰まりどころになった。

## 次の作業

- 再開する場合は、レビュー対応の前に契約面を 1 つ固定する。
- そのうえで `requirements.md`、`plan.md`、現在コードの公開 surface が揃う修正だけを入れる。
- `codex` は section 8 と section 11 をまとめて扱い、未実装のまま公開し続けるのか、公開面を閉じるのかを先に決める。
