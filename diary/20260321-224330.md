# 2026-03-21 22:43:30

## 実施内容

- `provider.py` を追加し、`ProviderSpec`、`ResolvedProvider`、`resolve_provider(...)` を実装した。
- provider 選択の責務を `preflight.py` から切り出し、明示指定・設定優先順位・秘密値確認を provider モジュールへ集約した。
- `session.py` を追加し、`SessionKey`、`ResolvedSession`、`SessionStateRef` を実装した。
- `review` は `review` lane、それ以外は `main` lane とし、session key を `<backend>/<target>/<provider>/<lane>` で組み立てるようにした。
- `--session` 未指定時は `inherit` を使うようにし、CLI の確認メッセージへ `session_mode`、`session_lane`、`session_key` を追加した。

## レビュー対応

- 自動レビューでは、provider / session の分離と CLI 側の接続に既存挙動を壊す明確な問題は見当たらないという結果だったため、追加修正は行わなかった。

## 手動確認

- `./.venv/bin/python -m compileall src`
- 一時ディレクトリ上の最小 `vv-ai.yml` で local issue target に対して `plan` を実行し、`provider=codex`、`session_mode=inherit`、`session_lane=main`、`session_key=local/issue:.../codex/main` が出ることを確認した。
- 一時ディレクトリ上の local pr target に対して `review` を実行し、`session_lane=review`、`session_key=local/pr:.../codex/review` が出ることを確認した。
- target を持たない `issue` コマンドで `session_key=local/command:issue/codex/main` が組み立てられることを確認した。
- `--provider claude` かつ `VV_ANTHROPIC_API_KEY` 未設定時に、日本語エラーで終了することを確認した。

## 次の作業

- `plan.md` の次の未完了タスクである「session の保存対象と復元対象を実装する」に進む。
