# 2026-03-21 21:42:34

## 実施内容

- `target` 解決を GitHub 専用実装から GitHub / local 共通実装へ広げた。
- `.vv-ai/issues/<id>` と `.vv-ai/issues/<id>/issue.md`、`.vv-ai/prs/<id>` と `.vv-ai/prs/<id>/pr.md` を local target として解決できるようにした。
- `ResolvedTarget` に local backend 用の `local_id` と `path` を追加し、canonical target を `issue:<id>` / `pr:<id>` にそろえた。
- CLI は repo root を resolver に渡し、GitHub URL と local path の両方で確認用出力に `target=` を含めるようにした。

## レビュー対応

- 自動レビューでは「相対 path を cwd 基準で解決すべき」という指摘があったが、今回は事前に合意した repo root 基準を仕様として採用し、挙動変更は行わなかった。

## 手動確認

- `./.venv/bin/python -m compileall src`
- 一時ディレクトリに `vv-ai.yml` と `.vv-ai/issues` / `.vv-ai/prs` の最小構造を作成し、local issue ディレクトリ指定で `target=issue:login-403-7k2p9a` になることを確認した。
- 同じ一時ディレクトリで local PR の `pr.md` 指定から `target=pr:refactor-engine-1m4x8q` になることを確認した。
- GitHub Issue URL 指定が引き続き `target=octo/test#12` へ解決されることを確認した。
- `comments/`、`meta.json`、存在しない path を指定した場合に日本語の入力エラーで終了することを確認した。

## 次の作業

- `plan.md` の次の未完了タスクである「`.vv-ai/issues` と `.vv-ai/prs` の管理構造を実装する」に進む。
