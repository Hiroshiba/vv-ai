# 日誌 2026-04-01

## 実施内容

plan.md セクション 15「Codex Provider テスト」を開始した。

前回の tmux セッション `vvai-test` を削除して再作成し、age 鍵の環境変数をセットした上でテストを実行した。

エラーケース 3 件は全て通過した。

- C-01: 不正コマンド名 → exit 2 ✅
- C-02: reply で instruction なし → exit 2 ✅
- C-03: review で Issue を指定 → exit 1 ✅

C-10（reply ローカル Issue, dry-run）で以下のエラーが発生し、テストを中断した。

```
実行エラー: ProviderExecutionError: Codex が終了コード 1 で失敗しました:
Error loading config.toml: unknown variant `include_only`, expected one of `core`, `all`, `none`
in `shell_environment_policy.inherit`
```

`provider_execution.py` の `_build_codex_command()` で `-c shell_environment_policy.inherit=include_only` を渡しているが、実際の codex CLI はこのバリアントを認識しない。

## レビュー内容

ユーザーより「エラーが起きたらそれ以降の調査を終了して日誌を書くように」という指示を受けた。

## 見逃しの考察

`shell_environment_policy` の正しい設定値を実際の codex CLI で検証せずに実装していた。実装時点では codex CLI のドキュメントや実際の動作を確認せず、推測で `include_only` を使用した。

## 手こずったこと

特になし。エラーは明確で、原因も特定できている。次のタスクで codex CLI の実際のオプションを確認して修正する必要がある。
