# 日誌 2026-03-28 18:22

## タスク

主要シナリオの受け入れ確認を行う

## 実施内容

`tests/test_acceptance.py` を新規作成し、既存テストでカバーされていなかった 7 つの主要シナリオを統合テストとして追加した。

- implement コマンド Issue 起点 dry-run
- implement コマンド PR 起点 dry-run
- review コマンド PR 対象 dry-run
- review コマンド Issue 対象のバリデーションエラー
- issue コマンド dry-run
- issue_comment イベント経由の実行
- 未許可ユーザーの silent skip

## レビュー

### 1 回目

SHOULD-FIX 1 件: `_enter_common_patches` の `github_client: Any = None` がコーディング規約に違反。デフォルト値の付与と `Any` 型の使用。修正として型を `MagicMock` に変更し、デフォルト値を除去、呼び出し元 5 箇所で `MagicMock()` を明示渡しに変更した。

### 2 回目

指摘なし。全項目 CHECKED。

## 見逃しの考察

テストヘルパーの引数設計でプロダクションコードと同じ規約意識が抜けていた。テストコードでも「デフォルト値を与えない」「広い型を使わない」を意識する必要がある。

## 手こずったこと

`*_common_patches()` のアンパックを `with` 文内で使おうとして SyntaxError になった。Python の `with` 文では `*` アンパックが使えないため、`contextlib.ExitStack` パターンに切り替えた。
