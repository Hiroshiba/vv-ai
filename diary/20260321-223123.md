# 2026-03-21 22:31:23

## 実施内容

- `local_store` を追加し、`.vv-ai/issues`、`.vv-ai/prs`、`.vv-ai/artifacts` の基本ディレクトリを作成する処理を実装した。
- local Issue / PR の作成 API を追加し、`issue.md` / `pr.md`、`meta.json`、`comments/` をまとめて生成できるようにした。
- `meta.json` の最小スキーマを Pydantic で定義し、Issue は `open|closed`、PR は `open|closed|merged` の status を扱うようにした。
- local comment を `comments/YYYYMMDD-HHMMSS-<slug>.md` で保存する処理を追加した。
- local 実行時の `workflow_id` を `YYYYMMDD-HHMMSS-<6文字>` 形式で生成し、CLI の確認メッセージに表示するようにした。

## レビュー対応

- 自動レビューでは、今回の差分に既存挙動を壊す明確な不具合は見当たらないという結果だったため、追加修正は行わなかった。

## 手動確認

- `./.venv/bin/python -m compileall src`
- 一時ディレクトリで `create_local_issue(...)` を実行し、`.vv-ai/issues/<id>/issue.md`、`meta.json`、`comments/` が生成されることを確認した。
- 一時ディレクトリで `create_local_pr(...)` を実行し、`.vv-ai/prs/<id>/pr.md`、PR 用 `meta.json`、`comments/` が生成されることを確認した。
- `append_local_comment(...)` で `comments/YYYYMMDD-HHMMSS-<slug>.md` が生成され、本文が保存されることを確認した。
- `generate_local_workflow_id(...)` が `YYYYMMDD-HHMMSS-<6文字>` 形式になることを確認した。
- local CLI 実行時に確認用出力へ `workflow_id=` が含まれることを確認した。

## 次の作業

- `plan.md` の次の未完了タスクである「provider 抽象を定義する」に進む。
