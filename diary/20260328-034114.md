# 日誌 2026-03-28 03:41

## 実施内容

セクション11「Dry-run / Security」の4タスクを実装した。

- provider.py: ProviderSpec に api_key_file_env フィールドを追加し、ファイルパス優先・生キー値フォールバックの provider 秘密値チェックを実装
- provider_execution.py: _resolve_api_key と _resolve_api_key_file_path を追加し、workflow YAML のファイルパス env と Python 側の env var 名のミスマッチを解消。Codex に shell_environment_policy の include_only を設定。Claude Code settings に permissions.deny を追加
- artifact_crypto.py: _resolve_required_secret を同じファイルパス優先パターンに変更
- vv-ai.yml: TODO(task11) コメントを削除

## レビュー

MUST-FIX 2件:

1. _resolve_api_key_file_path が delete=False で一時ファイルを作るがクリーンアップ処理がなかった。戻り値を tuple[str, bool] に変更し _execute_claude 内で try/finally で削除するよう修正。
2. Codex の shell_environment_policy が未実装のまま TODO が削除されていた。_build_codex_command に -c shell_environment_policy.inherit=include_only と include_only リストを追加して修正。

## 見逃しの考察

1件目: 一時ファイルの作成と使用が別関数に分かれるとクリーンアップ責務が曖昧になる。delete=False を使った時点で「誰が消すか」を同時に決めるべきだった。
2件目: 計画段階で「Codex CLI の仕様に依存するためプロトタイプでは見送り」と判断したが、codex exec --help を確認すれば -c フラグで設定可能なことがわかった。見送り判断の前に CLI ヘルプを確認する手順が抜けていた。

## 手こずったこと

特になし。
