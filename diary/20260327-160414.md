# 日誌 2026-03-27 16:04

## 実施内容

`issue` コマンドを実装した。

- `prompt.py` に issue 用タスク説明を追加し、AI に TITLE/BODY フォーマットで出力させる指示を定義
- `command_handler.py` に `_parse_issue_output()` と `_handle_issue_post_execution()` を追加
- 後処理で AI 出力をパースし、GitHub Issue を作成、起点コメントがあればリンクを1行返信する流れを実装

## レビュー

レビュー指摘なし。

## 手こずったこと

特になし。既存コマンドのパターンが明確で、resolve.py / session.py での issue コマンド対応も既に完了していたため、追加実装は prompt.py と command_handler.py の2ファイルのみで済んだ。
