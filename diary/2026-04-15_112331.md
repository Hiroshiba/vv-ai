# Issue #12: `inherit_or_new` セッションモードの追加とデフォルト化

## レビュー内容

NOTE 3 件、すべて SKIP と判定。

- `session.py:209` `supports_session_resume=False` 時の挙動について寛容化の提案。現状全 provider が対応しており、来ない値の救済は書かない方針のため SKIP。
- `session.py:223-226` 壊れた manifest の扱い。Issue #12 の議論通り壊れた状態は握り潰さない設計で、テストで固定済みのため SKIP。
- `plan.md` の staged/unstaged 競合。最終状態は正しく、コミット時に両方 stage すれば意図通り反映されるため SKIP。

## 見逃しの考察

レビューで新規バグは発見されなかった。設計意図はレビューでも追認されており、レビュアーの NOTE も「将来のための検討事項」にとどまった。

## 手こずったこと

- Issue #12 のコメントスレッドには「`inherit_or_new` を増やす案」と「未指定時だけ特別扱いする案」が両方あり、どちらを採るか一見迷った。最後の Hiroshiba のコメント「codex君の言うことは鵜呑みにしなくていい・最適な設計で実装」を読み、公開値として `inherit_or_new` を加え `requested_mode`/`restore_strategy` の 2 層に分離する案を採用した。
- `_resolve_restore_state` の戻り値を 2 タプルから 3 タプルへ拡張する改修で、`strict_missing` フラグと `restore_strategy` の導出順序をどこに置くか迷った。結果として `if requested_mode == "new"` の早期 return 後、provider 能力チェックを挟み、そのあと strict フラグと strategy を導出する形に落ち着いた。
- plan.md のチェックマークを付けた際、`git status` で staged と unstaged に同じ行が別状態で現れた。差分を確認したところ辻褄は取れていたが、review でも指摘されたのでコミット時に両方を一括 stage する必要がある。
