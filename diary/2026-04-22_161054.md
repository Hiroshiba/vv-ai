# Issue #30: breakdown コマンド後処理の add_sub_issue 失敗処理

## レビュー内容

3件の指摘（MUST-FIX × 1、SHOULD-FIX × 2）。

- MUST-FIX: `add_sub_issue` 失敗時に `CommandError` でループ中断 → サマリコメント未投稿のまま終了するため、再実行すると同じタスクの Issue が重複する問題。`CommandError` を stderr 警告に変更して続行に修正した
- SHOULD-FIX: `_parse_breakdown_output` が本文中の `TASK:` 行も区切りとして誤認識する問題。対応しない。理由: ユーザーが「複数ファイル書き出し方式への変更」の TODO を追加済みで待機を指示されているため、現行パーサーへの局所修正は後方作業になる
- SHOULD-FIX: 先頭ブロックが `TASK:` なしでも `TITLE:` で始まれば受理してしまう問題。同上の理由で対応しない

## 見逃しの考察

- `add_sub_issue` 失敗時の動作が「コマンド全体失敗 → サマリコメント未投稿」になっていた点は、コード上は例外処理として正しく見えるため見落としやすかった。再実行時の重複という副作用まで追わないと気づけない
- 「エラーは伝播させよ」という規約を杓子定規に適用すると、外部副作用が絡む非アトミック処理で逆にユーザー不利になるケースがある点を改めて認識した

## 手こずったこと

なし。修正は1行で、テストも全通過した。
