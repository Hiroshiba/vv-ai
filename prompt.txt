requirements.mdを元に実装を進めます。

まずはrequirements.mdをすべて読んで、plan.mdを作ってください。
タスクリストになっていて、タスクが完了したらチェックを入れる形式でお願いします。

タスクを実装する粒度は任せます。
変更が 200 行前後で収まるなら、関連する修正は 1 タスク内でまとめて実行して良いです。

最終的に要件を満たすなら、途中で一時的な要件違反が残っていても良いです。
その場合はTODOコメントを書いてください。

現状plan.mdに書かれていることは実装が完了しました。
続いてテストを行っていきます。
実行確認のための手順を、plan.mdに書いてください。

まずcodex providerでテストをしていってください。
そのあとclaude providerでテストをしていってください。

ローカルの場合はAPI KEYをパスするようにしたいです。
つまり迂回方法を実装したいです。
ローカルのときは必ず迂回するのではなく、迂回するためのフラグを用意してください。

ローカルでclaudeとcodexを使って動作チェックする際、同じproviderからは動かせないのと、ログを確認しやすいというのもあって、tmuxを使ってください。
毎回最初に前回のtmuxセッションが残っていないか確認して、残っている場合は削除してください。

plan.mdを読み、次のタスクを決めてください。
動作チェックを失敗するか終わるまで続け、失敗した場合はそれ以降進めずに終了して日誌を書いてください。
指示にあること以外は一切行わないでください。

---

以上が以前のタスクです。
次のタスクを行ってください。

以下に関してはcontext7 mcpを使って適宜情報を得てください。
- github actions
- github runner
- github workflow
- codex
- claude code
- uv
