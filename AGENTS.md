コード内コメントやドキュメントやエラーメッセージは日本語で書く。
プロトタイプ段階では自動テスト実装を前提にせず、必要最小限の手動確認を考える。
変更が 200 行前後で収まるなら、関連する修正は 1 タスク内でまとめて実行してよい。
仕様外のフォールバックは禁止。ファイルが存在するはずなのに存在しない・jsonなはずなのにloadが失敗するなど、想定外の処理はエラーにする。フォールバックが望ましいと判断した場合は仕様に含めるべきか相談して判断を仰ぐ。
